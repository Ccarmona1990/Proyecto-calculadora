import mostrarResultado from "./components/mostrarResultado.js";
import agregarEventListener from "./components/agregarEventListener.js";

// Inicializamos el resultado en 0
mostrarResultado('0');
agregarEventListener;
